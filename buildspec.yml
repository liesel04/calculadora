version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm install
  pre_build:
    commands:
      - npm test || true
  build:
    commands:
      - node build.js

artifacts:
  files:
    - '**/*'
